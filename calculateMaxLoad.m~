function maxLoad = calculateMaxLoad(L, T, fitCoe, memberLengths)
    nMembers = length(memberLengths);
    SR = zeros(1,nMembers);
    
    for i = 1:nMembers
        SR(i) = T(i)./(fitCoe/(memberLengths(i)^2));
    end
    
    for j = 1:nMembers
        if (SR(j) <
        
    maxLoad = abs((sum(L) * (1 / SR(failMember(end)))));
    
end